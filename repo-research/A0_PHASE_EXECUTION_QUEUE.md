# A0 PHASE EXECUTION QUEUE

> **Status:** A0 future phase execution queue for ChatGPT Control Room. **Plan-only.** No code, no installs, no MCP setup, no lockfile edits, no `package.json` edits, no README edits, no security / booking fixes, no UIX0 / MOTION0 / TS0 / RDG0 / IMPL work started, no git init, no Ponytail / ECC install / clone / copy / configure / evaluation.
>
> **Updated 2026-06-16 (Control Room correction):** first baseline commit is **OPTIONAL**, not a precondition for Cleanup A. **No remote, no push, no GitHub repo, no publish** — default to no remote until final delivery approval. PR-style review is replaced by **phase-stop review**. The Setup phase ran on 2026-06-16 and produced `repo-research/SETUP_FIRST_COMMIT_PLAN.md`; the first commit is left to the owner (or to a future Setup-AGENT invocation explicitly told to commit). The full policy is in `PROJECT_CONTROL_LOG.md` under "Git push / commit policy update (2026-06-16) — Control Room correction."
>
> **Updated 2026-06-16 (Cleanup B executed):** Cleanup B ran on 2026-06-16. `pnpm-lock.yaml` was deleted; `package-lock.json` remains as the canonical lockfile; `pnpm-lock.yaml` is now in `.gitignore`; `package.json` is unchanged. Setup, Cleanup A, and Cleanup B have all run on 2026-06-16. The CI re-entry guard (asserting `pnpm-lock.yaml` is absent) is gated to a future TS0 / RDG0 phase. R-002 (the dual-lockfile risk) is **closed**.
>
> **Phase:** A0 — Action / Build Plan (companion to `docs/A0_ACTION_PLAN.md`).
>
> **Source materials:** `docs/PM1_PLAN.md`, `docs/PD1_DECISION_LOCK.md`, `docs/D0_ARCHITECTURE_DECISIONS.md`, `docs/D0_SYSTEM_DESIGN.md`, `repo-research/D0_IMPLEMENTATION_BOUNDARIES.md`, `repo-research/PM1_PHASE_SEQUENCE.md`, `docs/A0_ACTION_PLAN.md`, `PROJECT_CONTROL_LOG.md`, `INTEGRATION_NOTES.md`, all `memory/`, all `ai/`.
>
> **Hard rules respected:** no code, no installs, no MCP setup, no lockfile edits, no `package.json` edits, no README edits, no security / booking fixes, no A0 / UIX0 / MOTION0 / TS0 / RDG0 / IMPL work started, no git init, no Ponytail / ECC install / clone / copy / configure / evaluation.

## How to use this file

- Read the table in order. The order is opinionated; parallelism is allowed where the rows say so.
- Each row is its own gated phase. Phases do not start the next phase. They stop and wait for ChatGPT Control Room.
- All file ownership is governed by `repo-research/A0_CHANGE_ZONE_MAP.md` and the per-phase allowed zones in `repo-research/D0_IMPLEMENTATION_BOUNDARIES.md` §5.
- The `Hard rules` row at the bottom restates the absolute gates enforced across all phases.

## Phase queue table

| Order | Phase | Purpose | Depends On | Allowed Files Later | Forbidden Now | Control Room Gate |
|---|---|---|---|---|---|---|
| 1 | **Setup** | Confirm `F:\CodeOutfitters` is the real repo root. Decide whether to initialize git. Review `.gitignore`. Prepare first safe commit plan. **First commit is OPTIONAL** (per the 2026-06-16 Control Room correction). **No remote, no push, no GitHub repo, no publish** — default to no remote until final delivery approval. PR-style review is replaced by **phase-stop review**. | Q-21 / D-027 owner confirmation | `PROJECT_CONTROL_LOG.md`, `INTEGRATION_NOTES.md`, `.gitignore` (review), `docs/`, `memory/`, `ai/`, `repo-research/` | `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, `package.json`, both lockfiles, `tsconfig.json`, `next.config.*`, `postcss.config.*`, eslint config, tailwind config, `.env*`, `public/_headers`, `public/_redirects`, `README.md`, `DEPLOY.md` (root), `.github/`, `tests/`, `.mcp.json`, worker source, Supabase SQL files, n8n workflow docs; no `npm install` / `pnpm install` / `yarn install`; no `npx`; no `git init` in A0; no `git push`; no `git remote add`; no GitHub repo; no publish; no commit on the owner's behalf; no Ponytail / ECC install / clone / copy / configure / evaluation | Mandatory stop after the first commit plan exists (the Setup phase ran on 2026-06-16 and produced `repo-research/SETUP_FIRST_COMMIT_PLAN.md`), before any other phase may start. The first commit itself is OPTIONAL and is not a stop point. Subsequent phases (Cleanup A, Cleanup B, TS0 / RDG0, QA Slice 0, Observability) require the repo to be initialized (already done). They do not require a baseline commit. Cleanup B's "git status clean" rule is relaxed — the agent must not create commits to make `git status` clean. Each gated phase uses **phase-stop review**. |
| 2 | **Cleanup A** | README repair. `DEPLOY.md` cleanup. Portfolio copy truth fix. Contact form `source: "contact"` field. `tsconfig.tsbuildinfo` ignore plan. ESLint config plan if needed. | Setup; Q-13, Q-14 answered | `README.md` (surgical repair), `DEPLOY.md` (root; delete only), `app/(public)/portfolio/page.tsx` (copy only), `components/contact.tsx` (one field: `source: "contact"`), `.gitignore` (R-025 entry), `eslint.config.mjs` (new file; config-only), `docs/`, `memory/`, `ai/`, `repo-research/`, `PROJECT_CONTROL_LOG.md`, `INTEGRATION_NOTES.md` | `lib/`, `hooks/`, `public/`, `next.config.*`, `postcss.config.*`, `tsconfig.json`, `package.json`, both lockfiles, `.env*`, every other file under `app/`, `components/`; no new deps; no lockfile edits; no security / booking fix; no new animations; no `npx`; no Ponytail / ECC | Mandatory stop after each PR (or one PR if combined). Subsequent phases (Cleanup B, TS0 / RDG0, QA Slice 0, Observability) require Cleanup A completion. |
| 3 | **Cleanup B** | Lockfile cleanup. npm canonical decision. Remove `pnpm-lock.yaml` later if confirmed. Validate package manager workflow. | Cleanup A; D-015 answered; `git status` shows only the in-progress Cleanup B changes (and any prior local commits the owner has chosen to make); the agent must not create commits to make `git status` clean | The dropped lockfile (delete only); `.gitignore` (add the dropped lockfile); `docs/SETUP.md`, `docs/DEPLOYMENT.md` (prose only); `README.md` (already repaired in Cleanup A; this phase only updates the package manager reference if needed); if pnpm, `package.json` (add `"packageManager": "pnpm@<version>"` field only; gated); `docs/`, `memory/`, `ai/`, `repo-research/`, `PROJECT_CONTROL_LOG.md`, `INTEGRATION_NOTES.md` | `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, `next.config.*`, `postcss.config.*`, `tsconfig.json`, `.env*`; no `package.json` edit except the gated `packageManager` field; no CI config file; no Ponytail / ECC; no commit on the owner's behalf | Mandatory stop after the lockfile drop, the docs update, and the `package.json` `packageManager` change (if pnpm). The CI guard itself is added in a later TS0 / RDG0 gated phase. |
| 4 | **Security Phase 1 — Worker proxy for Anthropic** | Cloudflare Worker proxy for Anthropic proposal generation. Remove client-side Anthropic key from bundle later. | A0; Setup; D-019a confirmed | `lib/proposal-generator.ts`, `app/admin/proposal/page.tsx` (only if the fetch URL changes here), new Worker source, `public/_headers` (CSP update; gated), `docs/SECURITY.md`, `docs/ARCHITECTURE.md`, `INTEGRATION_NOTES.md`, `repo-research/RISK_REGISTER.md` (move R-002, R-004 to "Closed"), `memory/`, `ai/`, `PROJECT_CONTROL_LOG.md` | Every other file in `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, all config files except `public/_headers`; no new runtime deps; no lockfile changes; no `npx`; no Ponytail / ECC | Mandatory stop after the Worker ships and the smoke test passes, before Security 2 may start. |
| 5 | **Security Phase 2 — Admin auth** | Admin auth path. Cloudflare Access fast path. Supabase Auth only if admin becomes productized. | Security 1; D-020a confirmed | `app/admin/layout.tsx`, `app/admin/page.tsx` (only if the dashboard depends on the auth shape), `lib/supabase.ts` (Auth; if Supabase Auth path is used), `docs/SECURITY.md`, `docs/ARCHITECTURE.md`, `INTEGRATION_NOTES.md`, `repo-research/RISK_REGISTER.md`, `memory/`, `ai/`, `PROJECT_CONTROL_LOG.md`. Cloudflare Access policy is set at the Cloudflare dashboard; A0 / Security 2 documents the policy intent. | Every other file in `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, all config files; no paid tools; no lockfile changes; no Ponytail / ECC | Mandatory stop after the auth path ships and the smoke test passes, before Security 3 may start. |
| 6 | **Security Phase 3 — Supabase RLS** | Supabase RLS. RPC / service-role strategy if needed. | Security 1 or 2; D-020 confirmed; Supabase service-role key in the Worker (or available for the future Worker) | `lib/booking-schema.sql` (RLS policies, RPCs), `lib/booking-actions.ts` (RPC call shape, if changed), `docs/DATABASE.md`, `docs/SECURITY.md`, `docs/ARCHITECTURE.md`, `repo-research/RISK_REGISTER.md`, `memory/`, `PROJECT_CONTROL_LOG.md`. Supabase dashboard settings (RLS is enabled in the SQL; the SQL is the source of truth). | Every other file in `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, all config files; no paid tools; no lockfile changes; no Ponytail / ECC | Mandatory stop after RLS is enabled and the smoke test verifies the deny, before Booking B may start. |
| 7 | **Security Phase 4 — n8n per-form secret** | n8n per-form secret / header contract. | Security 1 or 2; n8n workflow is ready to accept the header (out-of-repo; documented) | `components/quote-form.tsx`, `components/contact.tsx`, `components/booking-calendar-custom.tsx`, `components/newsletter.tsx` (add the header), the n8n workflow (out-of-repo; documented in `INTEGRATION_NOTES.md`), `INTEGRATION_NOTES.md`, `docs/SECURITY.md`, `repo-research/RISK_REGISTER.md`, `memory/`, `PROJECT_CONTROL_LOG.md` | Every other file in `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, all config files; no new runtime deps; no lockfile changes; no Ponytail / ECC | Mandatory stop after the header check is live in n8n and the unsigned-request test fails as expected. |
| 8 | **Booking Phase A — MVP** | UI reads available slots. Prevent visible double-booking. Clarify n8n booking flow. Confirm whether UI calls `createBooking`. | D0; A0; Setup; D-019b confirmed; Booking A write path (A first, then C) confirmed | `components/booking-calendar-custom.tsx`, `lib/booking-actions.ts` (RPC or guarded insert/update; do not change the anon-key write path in a way that breaks the Worker plan), `lib/booking-schema.sql` (re-seed script, if scheduled-function path is chosen), `docs/DATABASE.md`, `docs/ARCHITECTURE.md`, `repo-research/RISK_REGISTER.md`, `memory/`, `PROJECT_CONTROL_LOG.md` | Every other file in `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, all config files; no new animation; no new deps; no lockfile changes; no Ponytail / ECC | Mandatory stop after the UI reads slots and the write path is wired honestly, before Booking B may start. |
| 9 | **Booking Phase B — Robust** | Robust reservation path. RPC + Worker. Unique constraint / transactional reservation. | Booking A; Security 1; D-019b robust path confirmed | `lib/booking-schema.sql` (RPC, unique constraint), `lib/booking-actions.ts` (RPC call shape), Worker source (or a follow-up to the Worker from Security 1), `docs/DATABASE.md`, `docs/ARCHITECTURE.md`, `INTEGRATION_NOTES.md`, `repo-research/RISK_REGISTER.md`, `memory/`, `PROJECT_CONTROL_LOG.md` | Every other file in `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, all config files; no new runtime deps; no lockfile changes; no Ponytail / ECC | Mandatory stop after the RPC ships and the concurrent-submit test passes under load, before Admin features may run. |
| 10 | **Observability Phase** | Sentry candidate. UptimeRobot candidate. Email alerts. Webhook / booking / proposal failure visibility. | Cleanup A; D-026, D-021b confirmed; owner channel picked. Can run in parallel with Cleanup B, TS0 / RDG0, QA Slice 0 once Cleanup A is in. | `components/error-boundary.tsx` (Sentry wire), `lib/booking-actions.ts` (Sentry wrap), new monitor scripts (e.g. `scripts/seed-check.mjs`), n8n workflow (out-of-repo; documented in `INTEGRATION_NOTES.md`), `docs/SECURITY.md`, `INTEGRATION_NOTES.md`, `repo-research/RISK_REGISTER.md`, `memory/`, `PROJECT_CONTROL_LOG.md` | Every other file in `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, all config files; no paid tools; no new runtime deps; no lockfile changes; no Ponytail / ECC | Mandatory stop after each wire ships (Sentry, UptimeRobot, n8n monitor, booking wrap, proposal wrap), before the next wire. |
| 11 | **QA Slice 0** | Typecheck / lint plan. ESLint config plan. GitHub Actions plan. | Cleanup A; parallel with Cleanup B, TS0 / RDG0, Observability | `package.json` (script field only; no new dep), `eslint.config.mjs` (already added in Cleanup A; QA Slice 0 confirms wiring), `.github/workflows/ci.yml` (new; CI config), `docs/`, `memory/`, `ai/`, `repo-research/`, `PROJECT_CONTROL_LOG.md`, `INTEGRATION_NOTES.md` | `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, `next.config.*`, `postcss.config.*`, `tsconfig.json`, both lockfiles, `.env*`, `public/_headers`, `public/_redirects`, `.gitignore`; the CI workflow must not run any command that modifies the lockfile; no Ponytail / ECC | Mandatory stop after CI is green on a real PR, before QA Slice 1 may start. |
| 12 | **TS0 / RDG0 Tooling Approval Phase** | Plan exact evaluation and approval process for Repomix, Graphify, Context7 MCP, Playwright MCP, Chrome DevTools MCP, Impeccable, Emil Kowalski / Agents with Taste, Tree-sitter, codebase-memory MCP, Ponytail, ECC / affaan-m/ecc. | Cleanup A; PD1 + D0 + A0 pass | `repo-research/TS0_RDG0_REQUEST.md` (new; or split into per-tool PRs), `docs/`, `memory/`, `ai/`, `repo-research/`, `PROJECT_CONTROL_LOG.md` | `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, all config files, `package.json`, both lockfiles, `.env*`; no install; no MCP config; no `.mcp.json` write; no `devDependencies` change; no Ponytail / ECC install / clone / copy / configure / evaluation | Mandatory stop after ChatGPT Control Room returns per-tool approve / repair / reject decisions, before TS0 setup may start. |
| 13 | **QA Slice 1** | Real Playwright runner later. CI runs the smoke spec on every PR and every deploy. | TS0 setup; QA Slice 0; D-021a confirmed | `package.json` (devDep; Playwright), `playwright.config.*` (new), `tests/` (new; smoke spec only), `.github/workflows/ci.yml` (add the Playwright job), `docs/QA_CHECKLIST.md`, `memory/`, `PROJECT_CONTROL_LOG.md` | Every other file in `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, all config files; no mutating tests; smoke only; no Ponytail / ECC | Mandatory stop after the smoke spec is green on a real PR, before QA Slice 2 may start. |
| 14 | **QA Slice 2** | Lighthouse CI budgets. Playwright visual regression. Chrome DevTools MCP integration. | QA Slice 1; TS0 setup; D-021a, D-022a confirmed; Chrome DevTools MCP approved in TS0 / RDG0 | `package.json` (devDeps; Lighthouse, Playwright plugins), `tests/` (visual regression specs), `.github/workflows/ci.yml` (Lighthouse job), `.lighthouserc.*` (new; budget config), `repo-research/`, `memory/`, `PROJECT_CONTROL_LOG.md` | Every other file in `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, all config files; the pixel-delta gate is enforced in CI; manual runs are allowed; no Ponytail / ECC | Mandatory stop after the budget is enforced and the visual regression is green on a real PR, before UIX0 / MOTION0 first slice may start. |
| 15 | **QA Slice 3** | Form / booking / admin / accessibility tests later. Bundle size guard. | QA Slice 1; the feature phases (form, booking, admin); D-021a confirmed | `tests/` (new specs; mocks for Anthropic, n8n, Supabase), `package.json` (devDeps; axe-core, size-limit, etc.), `.github/workflows/ci.yml` (new jobs), `repo-research/`, `memory/`, `PROJECT_CONTROL_LOG.md` | Every other file in `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, all config files; no flaky tests; no Ponytail / ECC | Mandatory stop after the slice 3 tests are green on a real PR, before UIX0 / MOTION0 first slice may start. |
| 16 | **UIX0 / MOTION0 Planning Phase** | Convert D-011 and D-012 into a motion plan. Heavy premium agency-grade animations inspired by BeFluence reference only. Impeccable / Emil Kowalski review later. Performance budget. Reduced motion. Mobile rules. Admin lighter motion. | QA Slice 2; TS0 setup; D-021, D-022a, D-022, D-024, Q-17 confirmed | `docs/`, `repo-research/`, `memory/`, `ai/`, `PROJECT_CONTROL_LOG.md` | `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, all config files; no `npx`; no `npx skills add`; no `npx impeccable install`; no MCP setup; no animation library installs; no Ponytail / ECC | Mandatory stop after the motion plan is approved by ChatGPT Control Room, before the first slice may start. |
| 17 | **UIX0 / MOTION0 Implementation Phase (first slice)** | First slice: hero, headline reveal, scroll reveals, ROI micro-interactions, reduced-motion coverage. | UIX0 / MOTION0 Planning; QA Slice 2; TS0 setup | `components/hero.tsx` (animation), `components/aos-provider.tsx` (reduced-motion opt-out), `hooks/useScrollReveal.ts` (collapse duplicates; respect reduced-motion), `components/roi-calculator.tsx` (micro-interactions), `lib/animations/` (collapse to one canonical setup; respect reduced-motion), `docs/FEATURES.md`, `docs/ARCHITECTURE.md`, `docs/QA_CHECKLIST.md`, `repo-research/MOTION_QA_LOG.md` (new), `repo-research/RISK_REGISTER.md`, `memory/`, `PROJECT_CONTROL_LOG.md` | Every other file in `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, all config files; no new animation libraries; no `package.json` deps; AOS opt-out; GSAP opt-out; Framer Motion opt-out; LCP / CLS / INP within budget; no Ponytail / ECC | Mandatory stop after the first slice ships and the smoke test + taste review pass, before UIX0 / MOTION0 later slices may start. Not allowed until A0, TS0 / RDG0, QA baseline, and Control Room approval. |
| 18 | **UIX0 / MOTION0 Implementation Phase (later slices)** | Later slices: parallax, magnetic buttons, portfolio motion depth, process timeline animation, contact / booking form transitions, marquee polish, animated statistics counters. | UIX0 / MOTION0 first slice; per-slice owner approval; D-022a reaffirmed | `components/`, `hooks/`, `lib/animations/`, `docs/`, `memory/`, `PROJECT_CONTROL_LOG.md`. Each slice is a separate PR. Each PR may target a subset of these files. | All config files; no new deps without TS0 / RDG0 approval; benchmarks per slice; no Ponytail / ECC | Mandatory stop after each slice ships, before the next slice. |
| 19 | **Admin Future Phase** | Recent Proposals viewer. Proposal persistence. Better admin data model. | Security 2; Booking A; D-025 confirmed | `app/admin/page.tsx` (list view), `app/admin/proposal/page.tsx` (persistence on generate), `components/admin/` (list component, if new), `lib/proposal-generator.ts` (if persistence changes the flow), `lib/supabase.ts` (proposals table; if Supabase path is chosen), `docs/ARCHITECTURE.md`, `INTEGRATION_NOTES.md`, `repo-research/RISK_REGISTER.md`, `memory/`, `PROJECT_CONTROL_LOG.md` | Every other file in `app/`, `components/`, `hooks/`, `lib/`, `public/`, `styles/`, all config files; no new animations on admin; no new runtime deps; no lockfile changes; no Ponytail / ECC | Mandatory stop after A (or B) ships, before the admin motion discipline work. |
| 20 | **Final QA / Delivery Phase** | Full manual QA. Automated smoke. Deploy checklist. Cloudflare post-deploy checks. | All above; all launch gates green; release approval | `docs/QA_CHECKLIST.md`, `docs/DEPLOYMENT.md`, `memory/EPISODIC_MEMORY.md`, `PROJECT_CONTROL_LOG.md`. Verification only — no new work. | Every other file; no new work in this phase; no production operations; no Ponytail / ECC | Mandatory stop after release; subsequent maintenance is outside the A0 queue. |

## Parallelism

- #6 (Cleanup B) and #7 (TS0 / RDG0) and #9 (QA Slice 0) and #10 (Observability) can run in parallel after #2 (Cleanup A).
- #8 (Observability) can start as soon as #2 (Cleanup A) is in and the owner channel is picked (D-026).
- #11 (A0) gates the security / booking / UIX0 / MOTION0 implementation phases. A0 is the last planning phase.
- #4 (Security 1 — Worker) is the foundation for #5 (Auth) and #9 (Booking B).
- #8 (Booking A) can run ahead of #5 (Auth) if the owner chooses the anon-key write path; otherwise it depends on the Worker.

## Hard rules (enforced across all phases)

- **No D0 before PM1 and PD1 pass.** D0 depends on PM1 review and PD1 review. D0 is already written; pending review.
- **No A0 before D0.** A0 depends on D0. A0 is already written; pending review.
- **No feature agents before A0.** IMPL phases depend on A0.
- **No package installs before TS0 / RDG0 approval.** TS0 setup is its own gated phase.
- **No UIX0 / MOTION0 implementation before PM1, D0, A0, and tooling decisions.** UIX0 / MOTION0 depends on PM1, D0, A0, and TS0 setup.
- **No lockfile deletion before Cleanup B.** Cleanup B is its own gated phase.
- **No README edits before Cleanup A.** Cleanup A is its own gated phase.
- **No security or booking fix before their respective gated phases.** Each is a separate phase with its own gate.
- **No D-011 / D-012 implementation outside an approved UIX0 / MOTION0 phase.** D-011 and D-012 are capture-only decisions until the UIX0 / MOTION0 first slice.
- **No copy / scrape / clone of `befluence.pro` or any reference site.** Reference only.
- **No install, clone, copy, configure, or evaluation of Ponytail before TS0 / RDG0 approval.** Ponytail is a candidate only; not approved; gated to TS0 / RDG0.
- **No install, clone, copy, configure, or evaluation of ECC / affaan-m/ecc before TS0 / RDG0 approval.** ECC is a candidate only; not approved; gated to TS0 / RDG0.
- **No git init or git-changing command in any pre-Setup phase.** Setup is its own gated phase; only Setup runs `git init`, and only after the owner confirms the repo root (Q-21 / D-027).
- **A phase does not start the next phase.** It stops and waits for ChatGPT Control Room.
- **No code, no installs, no MCP config, no `.mcp.json` write, no `devDependencies` change, no global tool install, no `npx`, no `npm install`, no `pnpm install`, no `yarn install`, no `package.json` change in any pre-approval phase.**

## File ownership summary (allowed zones per phase)

| Zone | Owner phases |
|---|---|
| `docs/` | most phases; PM1, PD1, D0, A0, IMPL, all cleanup, security, booking, observability, UIX0 / MOTION0, QA |
| `memory/` | most phases; append-only for `EPISODIC_MEMORY.md`, `IMPORTANT_DECISIONS.md`, `WORKING_MEMORY.md`; rewrite OK for `CURRENT_STATE.md`, `ACTIVE_TASK_CONTEXT.md` (most-recent-writer-wins) |
| `ai/` | PM1 / PD1 / D0 / A0 (read-only on rules; can update `AI_TASK_CAPSULE.md`, `AI_CONTEXT_RULES.md`); IMPL (can update `AI_REPO_MAP.md`, `AI_FILE_OWNERSHIP.md`); DOC-MEMORY-REPAIR (initial creation) |
| `repo-research/` | any phase; planning scratchpad |
| `PROJECT_CONTROL_LOG.md` | any phase (append-only) |
| `INTEGRATION_NOTES.md` | any phase (append / update prose) |
| `package.json` | IMPL phases only; `packageManager` field gated to Cleanup B (if pnpm); script field gated to QA Slice 0 |
| Both lockfiles | Cleanup B only (drop one) + TS0 setup (regenerate if needed) |
| `tsconfig.json` | IMPL phases only; gated |
| `next.config.mjs`, `postcss.config.mjs`, `components.json` | IMPL phases only; gated |
| `.env*` | IMPL phases only; gated |
| `public/_headers` | IMPL phases only; gated; CSP changes require Security 5 (CI guard) |
| `public/_redirects` | IMPL phases only; gated |
| `app/**`, `components/**`, `hooks/**`, `lib/**` | IMPL phases only; gated per workstream |
| `README.md` | Cleanup A only; gated |
| `DEPLOY.md` (root) | Cleanup A (delete per Q-13) or Cleanup B; gated |
| `.gitignore` | Setup (review), Cleanup A (R-025), Cleanup B (dropped lockfile), IMPL (rare); gated |
| `.github/**` | QA Slice 0+; gated |
| `tests/**` | QA Slice 1+; gated |
| `.mcp.json` | TS0 setup only; gated per-tool approval |
| Worker source | Security 1, Booking B; gated |
| Supabase SQL files | Security 3, Booking A, Booking B; gated |
| n8n workflow docs | Security 4, Observability; gated |

## Safety confirmation (this file)

- This file is documentation only. No code, no installs, no config.
- No source / runtime / config files were modified to produce this file.
- This file lives in the allowed change zone (`repo-research/`).
- No Ponytail / ECC install / clone / copy / configure / evaluation.
- No git init.
- No `npx` or package manager command.
