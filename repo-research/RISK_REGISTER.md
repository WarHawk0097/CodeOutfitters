# RISK REGISTER

Every known risk in the project, in a single structured table. Severity and likelihood use a 1-5 scale (5 = worst). Required gate refers to which approval gate must clear before any fix is allowed.

| Risk ID | Risk | Area | Severity | Likelihood | Evidence | Impact | Proposed Future Fix | Required Gate | Owner Decision Needed? |
|---|---|---|---|---|---|---|---|---|---|
| R-001 | README is inaccurate (port 3000 vs 3005, v0 boilerplate, wrong edit hint) | docs | 2 | 5 | `README.md` vs `package.json` script vs `docs/SETUP.md` | Operator confusion on first run; new contributors hit wrong port. | Surgical README repair per `repo-research/README_REPAIR_SPEC.md`. | PM1 | no |
| R-002 | Dual lockfiles (`package-lock.json` + `pnpm-lock.yaml`) | config | 3 | 5 | Both committed at repo root. | Install-time drift, CI parity issues, larger repo. | Pick one manager, delete the other, document. | PM1 | yes (which one) |
| R-003 | Admin password is `NEXT_PUBLIC_ADMIN_PASSWORD`, in the static bundle | security | 4 | 5 | `app/admin/layout.tsx:21,33`, `next.config.mjs` `output: 'export'`, `.env.local.example:5` | Anyone can read the source and bypass the gate. | Add a real auth flow (Cloudflare Access / Auth.js / Worker auth). | PM1 + Control Room | yes (which auth) |
| R-004 | Anthropic API key is `NEXT_PUBLIC_ANTHROPIC_API_KEY`, in the static bundle | security | 5 | 5 | `lib/proposal-generator.ts:6`, `.env.local.example:4` | API key is public; usage is billed to the owner. | Proxy the call through a Cloudflare Worker. | PM1 + Control Room | yes |
| R-005 | Booking UI does not read `available_slots.is_booked`; double-book possible | product | 4 | 4 | `components/booking-calendar-custom.tsx:26-30` (no call to `getAvailableSlots`) vs `lib/booking-actions.ts` | Two visitors can book the same slot; second flips the flag but both rows exist. | **Status (2026-06-16):** read path fixed in Booking A (`public.get_available_slots` RPC, applied and verified at runtime by the owner); write path fixed in Booking B (`public.reserve_slot` RPC + `workers/booking-reservation-worker.ts`; row lock + `UNIQUE (preferred_date, preferred_time)` constraint). R-005 is **code-shipped at both read and write path levels** as of 2026-06-16. R-005 is **fully closed** only after the owner applies the Booking B SQL migration and deploys the Booking Worker (deployment steps in `repo-research/BOOKING_B_RESERVATION_RPC_WORKER_NOTES.md` §7). | PM1 | yes (fix shape) |
| R-006 | Supabase schema does not enable RLS | security | 4 | 5 | `lib/booking-schema.sql` (no `ALTER TABLE ... ENABLE ROW LEVEL SECURITY`) | Anon key has full read/write on `bookings` and `available_slots`. | Enable RLS, deny all to anon, allow insert via RPC. | PM1 | yes |
| R-007 | Supabase slot seed is 12 weeks from `2026-05-18` | ops | 3 | 5 | `lib/booking-schema.sql:31` (literal date) | Calendar will empty out around 2026-08-10. | Re-seed or build a scheduled function. | PM1 | no |
| R-008 | No tests in repo | QA | 3 | 5 | No `*.test.*` / `*.spec.*` files; `@playwright/test` is transitive only. | No safety net for future changes. | Add Playwright smoke + component tests in tooling-approved phase. | PM1 + TS0/RDG0 | yes |
| R-009 | No CI | QA | 3 | 5 | No `.github/`, no `.gitlab-ci.yml`, etc. | No automated checks on PR. | Add a minimal CI in tooling-approved phase. | PM1 + TS0/RDG0 | no |
| R-010 | No monitoring | ops | 3 | 5 | No error tracking, no uptime, no webhook monitor. | Silent failures. | Add Sentry/GlitchTip + UptimeRobot + n8n delivery monitor. | PM1 + TS0/RDG0 | yes (vendor) |
| R-011 | Developer tooling (Playwright MCP, Chrome DevTools MCP, Graphify, Repomix, Context7, Tree-sitter, codebase-memory) not approved | tooling | 2 | 5 | No MCP config; nothing in `package.json` `devDependencies`. | Cannot run automated visual/smoke QA loop. | Submit TS0 / RDG0 request in PM1. | PM1 + TS0/RDG0 | yes (which first) |
| R-012 | D-011 premium motion not planned; D-012 design taste not adopted | product | 2 | 4 | Captured in D-011, D-012; no plan file yet. | Either ship motion without performance budget, or never ship. | Produce UIX0 / MOTION0 plan in PM1. | PM1 | yes (priority vs perf) |
| R-013 | Animation performance risk: GSAP + ScrollTrigger + `@gsap/react` + Framer Motion + AOS + Lenis already in stack | performance | 3 | 4 | `package.json` deps. | Bundle bloat, jank on mid-tier mobile. | Audit + budget in UIX0 plan; remove duplicates; AOS reduced-motion opt-out. | PM1 + UIX0 | no |
| R-014 | Mobile performance risk | performance | 3 | 4 | Heavy static JS for animations; smooth scroll on low-end devices. | INP regressions, bounce. | Performance budget per UIX0 plan; benchmark on Moto G Power class. | PM1 + UIX0 | no |
| R-015 | Accessibility / reduced-motion risk | a11y | 2 | 4 | `components/aos-provider.tsx` does not pass a `disable` flag; AOS default has no `prefers-reduced-motion` opt-out. | Users with motion sensitivity may have a poor experience; potential WCAG 2.1 violation. | AOS opt-out + verify all motion respects the media query. | PM1 + UIX0 | no |
| R-016 | Static export limitations | architecture | 3 | 5 | `next.config.mjs` `output: 'export'`. No API routes, no middleware, no server actions, no edge functions in repo. | Cannot run any server logic; all secrets are in the bundle. | Decide path: keep + Worker, or drop static. See `repo-research/SECURITY_HARDENING_BRIEF.md`. | PM1 | yes |
| R-017 | n8n single-webhook contract risk | product | 3 | 4 | 4 public forms share one URL; payload shape discrimination only. | Misrouted leads if n8n misconfigured. | Per-form webhook URL or per-form secret + header. | PM1 | no |
| R-018 | Admin `localStorage` persistence limitation | security/product | 3 | 4 | `app/admin/onboarding/page.tsx`, `app/admin/proposal/page.tsx`, `app/admin/layout.tsx`. | Shared browser = shared data; data lost on browser reset. | Persist proposals to Supabase; harden auth. | PM1 | yes |
| R-019 | Portfolio truth gap (sample projects displayed, metadata says "real case studies") | copy | 2 | 5 | `app/(public)/portfolio/page.tsx` metadata vs `components/portfolio.tsx` "Sample Project" badge. | Visitor-facing contradiction, SEO contradiction. | Update metadata copy or replace sample data. | PM1 | yes |
| R-020 | CSP must be updated when new external endpoints are added | config | 2 | 4 | `public/_headers` `connect-src` is tight. | Production block on new endpoint if `next dev` does not catch it. | Add a CI lint or a checklist item. | PM1 | no |
| R-021 | Three GSAP entry points + duplicate `useScrollReveal` hooks | code hygiene | 2 | 5 | `lib/gsap.ts`, `lib/animations/gsap-setup.ts`, `lib/animations/gsap-config.ts`; `hooks/useScrollReveal.ts` vs `lib/animations/useScrollReveal.ts`. | Confusion, dead code, bundle bloat. | Collapse to one canonical setup. | PM1 | no |
| R-022 | AOS double-init risk | code | 1 | 3 | `components/aos-provider.tsx` calls `AOS.init` in a `useEffect` with no remount guard. | Mild: not a real risk in current page shape, but worth knowing. | Document; not urgent. | none | no |
| R-023 | `app/(public)/contact/page.tsx` description has stray spaces | copy | 1 | 5 | Line 17 in source. | Tiny but visible. | Repair in PM1 (low priority). | PM1 | no |
| R-024 | No `tsconfig.json` path to `components/ui/` even though `components.json` references it | config | 1 | 5 | `components.json` aliases `ui` → `@/components/ui`, but no such directory exists. | shadcn/ui generation would fail if attempted. | Document; do not generate yet. | none | no |
| R-025 | `tsconfig.tsbuildinfo` committed to repo | repo hygiene | 1 | 5 | Repo root. | Cluttered repo. | Add to `.gitignore` after lockfile resolution. | PM1 | no |
| R-026 | `package.json` has `lint` script but no ESLint config | config | 2 | 5 | `package.json:9`, no `eslint.config.*` in repo. | `npm run lint` does not work today. | Add minimal ESLint config in PM1. | PM1 | no |
| R-027 | `n8n` contact form payload lacks `source` field | integration | 2 | 5 | `components/contact.tsx:69-74` vs `components/quote-form.tsx:84-93` (which sets `source: "quote_request"`). | n8n must discriminate on absence-of-source; brittle. | Add `source: "contact"` to contact form. | PM1 | no |
| R-028 | CSP allows `https://cdn.simpleicons.org` for `img-src`; if CDN is down, tools strip is empty | product | 1 | 3 | `public/_headers`, `components/tools-strip.tsx:2-9`. | Marketing visual gap. | Self-host icons or accept. | none | no |
| R-029 | Admin password is the same value written to `localStorage.co_admin_auth` | security | 3 | 5 | `app/admin/layout.tsx:34`. | `localStorage` value is the password itself. | Hash or replace with a token. | PM1 + auth path | yes |
| R-030 | Static `out/` folder is gitignored but build size is untracked | ops | 1 | 5 | `.gitignore:21`. | Cannot tell from repo if build is healthy. | Add a build-size report in CI. | PM1 | no |
| R-031 | Hard-coded `2026-05-18` seed start in `lib/booking-schema.sql` | ops | 3 | 5 | `lib/booking-schema.sql:31,42`. | Seed will produce no slots if run after the window closes. | Re-seed logic or scheduled function. | PM1 | no |
| R-032 | No version pinning for the Anthropic model in code | ops | 1 | 3 | `lib/proposal-generator.ts:65` `claude-sonnet-4-6` is hard-coded. | Anthropic deprecates models; output drift. | Add env override. | PM1 | no |
| R-033 | `n8n` contact form lacks honeypot error UX parity | product | 1 | 4 | `components/contact.tsx:57` silently succeeds on honeypot. | Acceptable, but document. | Document; not urgent. | none | no |
| R-034 | `app/(public)/contact/page.tsx` has stray double-space ("audit on the spot  no obligation") | copy | 1 | 5 | Line 17. | Tiny but visible. | Repair in PM1. | PM1 | no |
| R-035 | Brand "All systems operational" footer status is hard-coded and never updated | product | 1 | 4 | `components/footer.tsx:57`. | Misleading if a real outage occurs. | Replace with live status badge. | PM1 | no |

## Cross-link summary

- Each roadmap item points to the risk IDs it mitigates: see `docs/ROADMAP.md`.
- The decision ledger entries reference their risks: see `memory/IMPORTANT_DECISIONS.md` (D-002 through D-012).
- The strategy briefs (security, booking, lockfile, README, tooling, UIX0, QA) all cross-reference the relevant risk IDs.

## Closed risks

Risks that are fully closed (runtime level) are moved here. Risks that are code-shipped but pending owner action (SQL apply, Worker deploy, etc.) stay in the main table with a "Status" note; the runtime closure is the owner's call.

| Risk ID | Closed at | Closed by | Final choice | Notes |
|---|---|---|---|---|
| R-001 | Cleanup A (2026-06-16) | CLEANUP-A-AGENT | README repaired per `repo-research/README_REPAIR_SPEC.md`. Port 3005 (not 3000). Entry `app/(public)/page.tsx`. Six env vars listed. Admin warning. Security warning. Foundation docs cross-linked. | ~104 lines. The admin password exposure (NEXT_PUBLIC_ADMIN_PASSWORD) is referenced in the security section but not fixed in Cleanup A; R-003 / R-029 are the real fix and are addressed by Security 2. |
| R-002 | Cleanup B (2026-06-16) | CLEANUP-B-AGENT | Lockfile = `npm` / `package-lock.json` per D-015 LOCKED DEFAULT. `pnpm-lock.yaml` deleted and added to `.gitignore`. | CI re-entry guard is **gated to a future TS0 / RDG0 phase** because it lives in `.github/workflows/`. The lockfile-level risk is closed; the CI guard is a follow-up. |
| R-019 | Cleanup A (2026-06-16) | CLEANUP-A-AGENT | Portfolio copy truth fix in `app/(public)/portfolio/page.tsx`. `metadata.description`, `PageHero` label, `PageHero` title, `PageHero` description changed from "Real case studies" to "Sample Scenarios" / "Sample Automation Scenarios" / "Illustrative examples...". | Copy-only. No layout change. No feature change. Consistent with the existing `SAMPLE WORK` overline and per-card "Sample Project" badges in `components/portfolio.tsx`. |
| R-023 | Cleanup A (2026-06-16) | CLEANUP-A-AGENT | Root `DEPLOY.md` deleted after verifying `docs/DEPLOYMENT.md` covers all of its content. `docs/DEPLOYMENT.md` header note updated to record the deletion. | `docs/DEPLOYMENT.md` is the canonical deploy doc going forward. |
| R-025 | Setup (2026-06-16) | SETUP-AGENT | `tsconfig.tsbuildinfo` added to `.gitignore`. | Confirmed in Cleanup A. |
| R-027 | Cleanup A (2026-06-16) | CLEANUP-A-AGENT | Contact form now sends `source: "contact"` in the payload. One-line edit in `components/contact.tsx`. | No refactor. No webhook URL change. No n8n integration change beyond the source field. `INTEGRATION_NOTES.md` §1 and `docs/ENVIRONMENT.md` updated additively. |
