# Cross-package integration tests

Per-package unit/smoke tests live beside their source (`apps/*/src`, `apps/web/app`). This directory holds tests that span multiple workspaces — none required yet in Phase 1.
