// Appointments screen — C-D11 (desktop), T-06 (tablet), MO-07 (mobile).
// Server component shell; AppointmentsScreen is the client island that owns the demo store,
// the view switch and every dialog.
import { AppointmentsScreen } from "./appointments-view";

export default function AppointmentsPage() {
  return <AppointmentsScreen />;
}
