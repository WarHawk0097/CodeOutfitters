# Local infrastructure

Compose file at `../docker/docker-compose.yml` defines local Postgres 16 and Redis for development. Not started automatically by any Phase 1 script or test — start manually when a later phase requires a running database/queue.
