# RISK REGISTER (remediated, Phase 0 decisions recorded)
Carries forward INTAKE-AND-ARCHITECTURE-ANALYSIS.md §9, plus risks found during direct inspection, repo audit, and Phase 0 decision closure.

| Risk | Detail | Mitigation direction |
|---|---|---|
| Pipeline Board native build | No CSV source component (Row 6 override); dnd-kit + keyboard sensor required from scratch. Confirmed in direct read: C-D06 uses a 4-col CSS grid, paginated ("STAGES 2-5 OF 11"), no prebuilt board library referenced anywhere in markup. | Isolate as its own phase/spike (PHASE 4 of implementation plan), budget extra time, build pagination logic alongside DnD since canonical frame shows both. |
| Zero backend/providers | Every data-bearing screen currently un-backed; confirmed no `app/api/` directory exists at all. | Mock/contract-first phase (msw against api-contracts.json) before real providers exist — PHASE 2. |
| Human-approval gates | 6+ mutation flows require explicit confirm/approval UI. Confirmed in markup: M-D18 explicitly renders "REQUIRES PROVIDER"/paused states rather than hiding them. | Each gated flow needs its own state-machine-accurate implementation; no "just show success" shortcuts. |
| AI safety constraints | No invented pricing/case studies/sentiment; client surfaces must hide AI provenance. | Enforce at both generation layer and render layer. |
| Location ambiguity — RESOLVED | F:\CodeOutfitters already had an `app/admin/` area overlapping intent but with different, thinner implementation and different design tokens. | RESOLVED at Phase 0 decision closure: Command Center is a separate isolated workspace (`command-center/`), not an extension of `app/admin/`. See ADR-COMMAND-CENTER-LOCATION.md, LEGACY-STUB-DISPOSITION.md. |
| Shared build/deploy surface — RESOLVED | Single Vercel project (prj_D8Z0xzQF8OWA0bsz0PGx7A8vYhrX) risked coupling Command Center's build/deploy to the public site's. | RESOLVED: Command Center is a separate workspace with its own package manifest/lockfile; root public app's package manager/lockfile is never touched to support it. Production deploy topology for the new services is deferred to a later phase, not decided here. |
| Missing `POST /api/leads` | Named integration point in docs, does not exist in this repo. | Treated as net-new work in phase plan (PHASE 2/9/15), not "already there, just wire it up." Real backend is now NestJS + Fastify inside `command-center/apps/api`, not a Next.js route handler. |
| Frame-level detail — mostly closed | Original risk: hash-verified but markup not read directly. Direct inspection closed sampled frames (C-D06, M-D03/04-18, T-01..11, MO-01..18 headers) — see work/CANONICAL-DC-DIRECT-INSPECTION.md. Not exhaustively re-verified line-by-line for all 49 frames. | Any phase touching a specific frame's exact markup should re-grep that frame's block before coding, not rely solely on this register. |
| **Multi-service architecture complexity (new)** | DECISION 2 commits to three deployable services (web/api/worker) plus Postgres/Drizzle/Redis-BullMQ, a materially larger surface than a single Next.js app. Local dev requires Docker Compose orchestration across services before any screen can be built end-to-end. | Phase 1 scaffolding must stand up the full local dev loop (docker compose for Postgres/Redis, workspace scripts to run web+api+worker together) before feature phases begin, not incrementally bolted on later. |
| **RLS owner-bypass risk (new)** | PostgreSQL table owners and superuser roles bypass row-level security by default. If the application's runtime database role is ever granted ownership or superuser, tenant isolation silently fails with no error. | Application role must be distinct, non-owning; use `FORCE ROW LEVEL SECURITY` where required; cross-workspace read/write blocking must be proven by integration tests using the real application role, not the migration/owner role. Re-verify at database-phase approval gate. See PHASE0-DECISION-CLOSURE.md §10. |
| **Production DB vendor deferred (new)** | No production Postgres vendor chosen; schema/migrations must stay portable across standard providers, which constrains use of any vendor-specific extension or feature during early implementation. | Avoid vendor-specific SQL/extensions in `packages/database` schema/migrations until a vendor is chosen; flag any proposed vendor-specific feature for explicit approval. |
| **Provider category expanded (new)** | Approved provider list grew from 5 to 8 adapters (added MeetingPlatformProvider, ObjectStorageProvider, ProposalAcceptanceProvider as explicit categories). | PROVIDER-ADAPTER-PLAN.md updated to cover all 8; mock-first strategy applies uniformly across all of them. |
