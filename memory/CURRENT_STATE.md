# Current State

Snapshot of the project as of the end of the Booking B batch.

## Project state

- **Phase:** A0 — Action / Build Plan (**APPROVED by ChatGPT Control Room as of 2026-06-16**). Setup phase (future phase #1) **run**. Cleanup A phase (future phase #2) **run** (2026-06-16). Cleanup B phase (future phase #3) **run** (2026-06-16). Security 1 phase (future phase #4) **run** (2026-06-16). Security 2 phase (future phase #5) **run** (2026-06-16). **Security 3 phase (future phase #6) run 2026-06-16; SQL migration applied and verified at runtime by owner**. Security 4 phase (future phase #7) **run** (2026-06-16; code + docs + CSP; no deploy; forms Worker source shipped; R-005 / R-017 addressed at the deployment level; F-006 implemented at the deployment level; deferred at the runtime level). **Booking A phase (future phase #8) run 2026-06-16; SQL migration applied and verified at runtime by owner; Booking A Repair 1 passed and applied to on-disk migration; Booking A live grant repair applied and verified at runtime by owner; R-005 partially closed at the read path level; F-004 implemented for the read path; write path (`createBooking`) intentionally unchanged and remains blocked until Booking B**. **Booking B phase (future phase #9) run 2026-06-16; SQL migration applied and verified at runtime by owner; Booking Worker deployed by owner via Cloudflare dashboard paste of `workers/booking-reservation-worker.dashboard.js`; `lib/booking-actions.ts:createBooking` replaced wholesale; `components/booking-calendar-custom.tsx:handleSubmit` updated to call `createBooking(formData)`; `.env.local.example` updated with `NEXT_PUBLIC_BOOKING_WORKER_URL`; R-005 fully closed at the runtime level; F-004 fully closed at the runtime level; Worker smoke test passed (`bookingId = 69e071f0-8954-4da1-bcb4-75f272bd2b87` with `notification = "skipped"`); Supabase verification confirmed the booking row exists and `available_slots.is_booked = true`; duplicate booking test returned `slot_already_booked` (`P0001`)**. The first commit is OPTIONAL. **Observability is the next eligible implementation phase, but is NOT started.** Observability is blocked until ChatGPT Control Room issues the exact Observability prompt. **Remaining deployment gap (informational, not a Booking B defect):** the Cloudflare Pages project is connected to GitHub; the current Pages deployment is still old GitHub code (pre-Booking-B frontend). The booking form's `handleSubmit` on the deployed Pages is the pre-Booking-B form. The Worker is end-to-end functional at the Worker level (smoke test passed); the browser-driven end-to-end flow cannot be live-tested through Pages until the updated local code is pushed/deployed. **Git push / remote remains blocked** until the owner explicitly approves final delivery deploy (per the 2026-06-16 Control Room correction on git push / commit policy).
- **Previous phases:** DOC-DISCOVERY (completed 2026-06-15), DOC-MEMORY-REPAIR (completed 2026-06-15), OVERNIGHT-SAFE-PRE8-AND-PM1-PREP (completed 2026-06-15), PM1 (written 2026-06-16; reviewed as part of A0; approved as part of A0), PD1 (written 2026-06-16; reviewed as part of A0; approved as part of A0), D0 (written 2026-06-16; reviewed as part of A0; approved as part of A0), D0 ECC addendum (written 2026-06-16; reviewed as part of A0; approved as part of A0), A0 (written 2026-06-16; **APPROVED by ChatGPT Control Room as of 2026-06-16**), Setup (run 2026-06-16), Cleanup A (run 2026-06-16), Cleanup B (run 2026-06-16), Security 1 (run 2026-06-16; code + docs + CSP; no deploy; R-002 closed; F-001 implemented), Security 2 (run 2026-06-16; code + docs; convenience gate explicitly labeled; R-001 addressed at deployment level; F-002 implemented for the deployed path; no Cloudflare Access app created in this phase), **Security 3 (run 2026-06-16; SQL migration applied and verified at runtime by owner; R-003 closed at the SQL level and the runtime level; F-003 implemented at the SQL level and verified at the runtime level; closed end-to-end)**, Security 4 (run 2026-06-16; forms Worker source `workers/n8n-form-proxy.ts`; four forms updated; CSP `connect-src` updated; R-005 / R-017 addressed at the deployment level; F-006 implemented at the deployment level; deferred at the runtime level), **Booking A (run 2026-06-16; SQL migration applied and verified at runtime by owner after Booking A Repair 1; Booking A live grant repair applied and verified; `lib/booking-actions.ts` `getAvailableSlots` calls the RPC; `components/booking-calendar-custom.tsx` uses the response; R-005 partially closed at the read path level and verified at the runtime level; F-004 implemented for the read path and verified at the runtime level; `createBooking` is intentionally unchanged and remains blocked until Booking B)**.
- **Gate:** IMPL phases **except Setup, Cleanup A, Cleanup B, Security 1, Security 2, Security 3, Security 4, and Booking A** (Security 5, Booking B, Observability, QA Slice 0..3, UIX0 / MOTION0 Planning, UIX0 / MOTION0 first slice, UIX0 / MOTION0 later slices, Admin future, Final QA / delivery), UIX0 / MOTION0 implementation, coding, and tooling installs are all **blocked**. The first commit is OPTIONAL. A0 is approved. Setup, Cleanup A, Cleanup B, Security 1, Security 2, Security 3, Security 4, and Booking A ran as deliberate, additive exceptions gated by the owner's prompts; they are not substitutes for A0 review (A0 is the review gate; they are IMPL phases that A0 planned).

## What is done

- Repository scanned in DOC-DISCOVERY. Full report produced.
- DOC-MEMORY-REPAIR foundation created.
- OVERNIGHT-SAFE-PRE8-AND-PM1-PREP batch completed:
  - PRE8 checkpoint written and passed
  - Documentation consistency audit written
  - Risk register expanded to 35 entries (R-001 to R-035)
  - PM1 input brief written
  - Strategy briefs written: README repair, lockfile, security, booking, tooling, UIX0/MOTION0, QA
  - Feature traceability matrix written
  - Agent boundary map written
  - Open questions register written
  - Working memory and important decisions updated with new decisions (D-013, D-014) and new open questions (Q-15 to Q-20)
- **PM1 batch completed (2026-06-16):**
  - `docs/PM1_PLAN.md` created (primary deliverable; 14 workstreams).
  - `repo-research/PM1_DECISION_MATRIX.md` created (D-15..D-27 detail).
  - `repo-research/PM1_PHASE_SEQUENCE.md` created (27 phases with gates and rules).
  - State files updated: `PROJECT_CONTROL_LOG.md`, `memory/CURRENT_STATE.md`, `memory/ACTIVE_TASK_CONTEXT.md`, `memory/WORKING_MEMORY.md`, `memory/EPISODIC_MEMORY.md`, `memory/IMPORTANT_DECISIONS.md` (D-015..D-027 added), `ai/AI_TASK_CAPSULE.md`, `ai/AI_CONTEXT_RULES.md`, `docs/51_AGENT_HANDOFF_LOG.md`.
- **PD1 batch completed (2026-06-16):**
  - `docs/PD1_DECISION_LOCK.md` created (primary deliverable; 13 PM1 decisions + 6 architectural-path decisions + 9 follow-up items normalized to a single ballot; lock-status legend; D0 readiness assessment; gates-still-blocked list).
  - `repo-research/PD1_OWNER_DECISION_BALLOT.md` created (optional support file; one row per decision for fast owner response).
  - State files updated: `PROJECT_CONTROL_LOG.md`, `memory/CURRENT_STATE.md`, `memory/ACTIVE_TASK_CONTEXT.md`, `memory/WORKING_MEMORY.md`, `memory/EPISODIC_MEMORY.md`, `memory/IMPORTANT_DECISIONS.md` (PD1 lock-tags appended to D-015..D-027; no new decisions), `ai/AI_TASK_CAPSULE.md`, `ai/AI_CONTEXT_RULES.md`, `docs/51_AGENT_HANDOFF_LOG.md`.
  - `INTEGRATION_NOTES.md` was not modified in this batch (no integration contract changed).
- **D0 batch completed (2026-06-16):**
  - `docs/D0_ARCHITECTURE_DECISIONS.md` created (primary deliverable; 12 areas; PD1 reflection table; D0 readiness assessment; gates-still-blocked list; safety confirmation).
  - `docs/D0_SYSTEM_DESIGN.md` created (primary deliverable; target architecture, security, booking, documentation / cleanup, QA / CI, tooling, UIX0 / MOTION0, admin, observability design diagrams and contracts; phase boundary design; D0 acceptance criteria; safety confirmation).
  - `repo-research/D0_IMPLEMENTATION_BOUNDARIES.md` created (primary deliverable; hard rules, allowed change zones, off-limits, phase-by-phase implementation boundaries, integration contract register, risk-to-phase mapping, rollback posture per phase, owner-confirmation checkpoints; safety confirmation).
  - State files updated: `PROJECT_CONTROL_LOG.md`, `memory/CURRENT_STATE.md`, `memory/ACTIVE_TASK_CONTEXT.md`, `memory/WORKING_MEMORY.md`, `memory/EPISODIC_MEMORY.md`, `memory/IMPORTANT_DECISIONS.md` (D0 reflection note appended; no new decisions), `ai/AI_TASK_CAPSULE.md`, `ai/AI_CONTEXT_RULES.md`, `docs/51_AGENT_HANDOFF_LOG.md`.
  - `INTEGRATION_NOTES.md` was not modified in this batch (no integration contract changed; D0 only plans integration architecture).
- **D0 ECC addendum completed (2026-06-16):**
  - `docs/D0_ARCHITECTURE_DECISIONS.md` updated with §11 "D0 ECC addendum"; row #30 in the major decisions reflection table; ECC row in the tooling architecture table; hard-rules-reaffirmed line; gates-still-blocked list; safety confirmation; recommended-next-step list; top header line; "Out of scope" line.
  - `docs/D0_SYSTEM_DESIGN.md` updated with §7.10 "ECC / affaan-m/ecc candidate rule (NOT APPROVED)"; renumbering of §7.10 → §7.11 and §7.11 → §7.12; phase boundary table; D0 acceptance criteria; safety confirmation; top header line.
  - `repo-research/D0_IMPLEMENTATION_BOUNDARIES.md` updated: hard-rules list; phase-by-phase TS0 / RDG0 entry; integration contract register note; risk-to-phase mapping; owner-confirmation checkpoints; safety confirmation; top header line.
  - `repo-research/TOOLING_APPROVAL_BRIEF.md` updated: new ECC row in §1 and §2; §5 "What Must Not Happen" extended; tool-purpose-matrix narrative.
  - State files updated: `PROJECT_CONTROL_LOG.md`, `memory/CURRENT_STATE.md`, `memory/ACTIVE_TASK_CONTEXT.md`, `memory/WORKING_MEMORY.md`, `memory/EPISODIC_MEMORY.md`, `memory/IMPORTANT_DECISIONS.md` (D0 ECC addendum section; D0 reflection note extended), `ai/AI_TASK_CAPSULE.md` (never-do list; PD1 tooling candidate section extended), `ai/AI_CONTEXT_RULES.md` (D0 ECC addendum tooling candidate rule), `docs/51_AGENT_HANDOFF_LOG.md`.
  - `INTEGRATION_NOTES.md` was not modified in this addendum (no integration contract changed).
- **A0 batch completed (this snapshot, 2026-06-16):**
  - `docs/A0_ACTION_PLAN.md` created (primary deliverable; current gate status; approved assumptions from PM1 / PD1 / D0; full 20-phase sequence with purpose, must-not-do, allowed file zones, owner decisions, acceptance criteria, rollback plans, Control Room stop points; hard rules reaffirmed; A0 acceptance criteria; safety confirmation; recommended next step).
  - `repo-research/A0_PHASE_EXECUTION_QUEUE.md` created (primary deliverable; the future phase queue table: Order, Phase, Purpose, Depends On, Allowed Files Later, Forbidden Now, Control Room Gate).
  - `repo-research/A0_AGENT_ASSIGNMENT_MATRIX.md` created (primary deliverable; the future agent assignment table: Agent, Future Phase, Responsibility, Allowed Files Later, Required Inputs, Stop Condition).
  - `repo-research/A0_CHANGE_ZONE_MAP.md` created (primary deliverable; the file-zone classification table: File / Zone, Phase Allowed, Why, Approval Required, Notes).
  - `INTEGRATION_NOTES.md` updated additively with §8 "A0 integration sequencing clarifications" (prose only; no breaking changes; clarifies the Anthropic Worker path, the n8n per-form secret, the Supabase RLS strategy, the observability alert channel, the git / repo root setup contract, the Ponytail / ECC candidate status, the admin persistence contract, and the UIX0 / MOTION0 first-slice contract).
  - State files updated: `PROJECT_CONTROL_LOG.md` (A0 batch overlay; "Current Phase" line updated to A0; phase history row added; gate status row updated), `memory/CURRENT_STATE.md` (this file), `memory/ACTIVE_TASK_CONTEXT.md` (task replaced with A0 — Action / Build Plan; definition of done updated), `memory/WORKING_MEMORY.md` (A0 plan summary; PD1 LOCKED DEFAULTS reflection table; architectural-path options reflection; bundled follow-up items reflection; Ponytail carried forward; D0 ECC carried forward; A0 readiness note), `memory/EPISODIC_MEMORY.md` (A0 event appended), `memory/IMPORTANT_DECISIONS.md` (A0 reflection note appended; no new decisions; existing D-IDs remain canonical; "PD1 lock-tags → Owner overrides" table extended for A0; "A0 reflection (2026-06-16) → Owner overrides" subsection added), `ai/AI_TASK_CAPSULE.md` (phase line updated to A0; A0 reflection note in the owner-direction summary), `ai/AI_CONTEXT_RULES.md` (A0 hard rule added; future-phase boundary rule reaffirmed; tooling-candidate rules reaffirmed), `docs/51_AGENT_HANDOFF_LOG.md` (A0 entry appended).
- **Setup batch completed (2026-06-16; first commit pending):**
  - Root confirmed: `F:\CodeOutfitters` is the real repo root. `pwd` returned `F:\CodeOutfitters` before any action.
  - Git initialized: `git init -b main` run at the confirmed root. No commits created. No remote added. No push.
  - `.gitignore` reviewed and repaired:
    - Corruption on line 15 (`.DS_Storenode_modules`) fixed — split into `.DS_Store` and `node_modules/`.
    - Missing safe entries added: `dist/`, `build/`, `tsconfig.tsbuildinfo`, `*.log`, `logs/`.
    - `node_modules/` normalized to trailing-slash form.
    - No broad patterns added; no source-file hides; no lockfile pre-emption; no `pnpm-lock.yaml` pre-ignore.
  - `repo-research/SETUP_FIRST_COMMIT_PLAN.md` created (primary deliverable; expected files, files NOT to commit, sensitive files, recommended commit message, manual owner commands, reversibility, gates-still-blocked, safety confirmation).
  - State files updated: `PROJECT_CONTROL_LOG.md` (Setup batch overlay; phase history row added; gate status row updated), `memory/CURRENT_STATE.md` (this file), `memory/ACTIVE_TASK_CONTEXT.md` (task replaced with Setup — A0 #1; definition of done updated), `memory/WORKING_MEMORY.md` (Setup phase added to phase gate; active restrictions line extended; Setup outputs section; next concrete action note; .gitignore repair note; git init note), `memory/EPISODIC_MEMORY.md` (Setup event appended), `memory/IMPORTANT_DECISIONS.md` (Setup reflection note appended; D-027 confirmed for `git init` only via Setup phase prompt), `ai/AI_TASK_CAPSULE.md` (Setup phase row added; D-027 Setup-specific line), `ai/AI_CONTEXT_RULES.md` (Setup hard rule added; git init rule reaffirmed; first commit gate rule added), `docs/51_AGENT_HANDOFF_LOG.md` (Setup entry appended).
  - `INTEGRATION_NOTES.md` was not modified (no integration contract changed in Setup; the git / repo root setup contract is already documented in §8).
  - No source file in `app/`, `components/`, `hooks/`, `lib/`, `public/` was modified.
  - No config file (`tsconfig.json`, `next.config.mjs`, `postcss.config.mjs`, `components.json`, eslint / tailwind configs) was modified.
  - No lockfile (`package-lock.json`, `pnpm-lock.yaml`) was modified or deleted.
  - No `package.json` was modified.
  - No packages were installed.
  - No tooling was configured.
  - No MCP server was configured.
  - No Ponytail or ECC / affaan-m/ecc install / clone / copy / configure / evaluation was performed.
  - No README or DEPLOY edits in this batch.
  - No test files created.
  - No CI files created.
  - No commit was created in this batch.

## What is blocked

- All IMPL phases **except** Setup, Cleanup A, Cleanup B, Security 1, Security 2, Security 3, Security 4, Booking A, and Booking B (all nine run on 2026-06-16; Security 3, Booking A, and Booking B are also **applied and verified at runtime** as of 2026-06-16) — Security 5, Observability, QA Slice 0..3, UIX0 / MOTION0 Planning, UIX0 / MOTION0 first slice, UIX0 / MOTION0 later slices, Admin future, Final QA / delivery. A0 is approved. The 20 future phases are listed in `repo-research/A0_PHASE_EXECUTION_QUEUE.md` and `docs/A0_ACTION_PLAN.md` §5. **Observability is the next eligible implementation phase** but is **not started** — it is blocked until ChatGPT Control Room issues the exact Observability prompt.
- Setup phase — first commit **optional**. Setup ran (`git init`, `.gitignore` repair, first commit plan) but the **first commit itself is not in this batch and is not required**. The owner may commit locally at any time, or defer all commits to final delivery. The agent does not commit on the owner's behalf.
- Cleanup A phase — run (2026-06-16). README repaired. `DEPLOY.md` deleted. Portfolio copy truth fixed. Contact form `source: "contact"` added. Integration notes updated. `.gitignore` confirmed clean. ESLint config deferred. Cleanup A does not block Cleanup B; Cleanup B is blocked on ChatGPT Control Room approval of Cleanup A and on D-015 answer.
- Cleanup B phase — run (2026-06-16). `pnpm-lock.yaml` deleted. `package-lock.json` remains as the canonical lockfile. `pnpm-lock.yaml` listed in `.gitignore` so it cannot reappear. `package.json` untouched. Documentation updated (README, `docs/SETUP.md`, `docs/DEPLOYMENT.md`, `repo-research/LOCKFILE_DECISION_BRIEF.md`, A0 plan files). R-002 (dual-lockfile risk) closed. CI re-entry guard gated to a future TS0 / RDG0 phase. Cleanup B does not block Security 1; Security 1 is blocked on ChatGPT Control Room approval of Cleanup B and on D-019a.
- Security 1 phase — run (2026-06-16). Cloudflare Worker proxy for Anthropic. Worker source in `workers/anthropic-proposal-proxy.ts`. Frontend `lib/proposal-generator.ts` now calls the Worker; no longer calls `https://api.anthropic.com` directly. `NEXT_PUBLIC_ANTHROPIC_API_KEY` is deprecated and forbidden. `NEXT_PUBLIC_PROPOSAL_WORKER_URL` is the new public env var. `ANTHROPIC_API_KEY` is the new server-side secret. CSP `connect-src` updated: `api.anthropic.com` removed; `*.workers.dev` added. Documentation updated (`docs/ENVIRONMENT.md`, `docs/SECURITY.md`, `docs/DEPLOYMENT.md`, `INTEGRATION_NOTES.md` §8.1). `repo-research/SECURITY_1_WORKER_PROXY_NOTES.md` is the primary deliverable. R-002 (Anthropic key in bundle) closed at the runtime level. F-001 implemented. CORS is not authentication; Security 2 is the next gate.
- Security 2 phase — run (2026-06-16). Cloudflare Access in front of `/admin/*` is the real admin boundary (D-020a LOCKED DEFAULT). The local client-side password gate in `app/admin/layout.tsx` is convenience-only and is explicitly labeled as such in the UI (a `ShieldCheck` notice on the login form and a chip in the admin header). `NEXT_PUBLIC_ADMIN_PASSWORD` is deprecated as a real security boundary. R-001 addressed at the deployment level. F-002 implemented for the deployed path. Worker-level session-token / Cloudflare Access JWT verification is a future hardening step, intentionally not shipped in Security 2. No new auth library is added (no Auth.js, no Supabase Auth, no server route, no new npm dependency). **No Cloudflare Access app was created in this phase**; the owner creates it in the Cloudflare Zero Trust dashboard before any non-internal launch. Setup steps are in `repo-research/SECURITY_2_CLOUDFLARE_ACCESS_NOTES.md` §3.
- **Security 3 phase — run (2026-06-16); SQL applied and verified at runtime by owner (2026-06-16).** Supabase RLS is required before any non-internal launch (D-020 LOCKED DEFAULT). The SQL migration is in `supabase/migrations/20260616_security3_rls.sql`. **The migration was applied and verified at runtime by the owner** (confirmed 2026-06-16). Policy model: deny all to anon on `bookings` and `available_slots`; full access to `service_role`; forward-compatible grants on the future narrow RPCs (`get_available_slots` for anon in Booking A; `reserve_slot` for `service_role` only in Booking B). Runtime state: `public.bookings` has `relrowsecurity = true` and `relforcerowsecurity = true`; `public.available_slots` has `relrowsecurity = true` and `relforcerowsecurity = true`. Four RLS policies exist: `anon_deny_all_available_slots`, `service_role_full_access_available_slots`, `anon_deny_all_bookings`, `service_role_full_access_bookings`. The base booking schema is in place; `public.available_slots` was seeded with 840 slots. R-003 is **closed at the SQL level** and **closed at the runtime level**. F-003 is **implemented at the SQL level** and **verified at the runtime level**. R-003 is closed end-to-end. No `service_role` key is in any `NEXT_PUBLIC_*` env var, in the static bundle, in the repo, or in any client-reachable file. The booking flow is non-functional by design until Booking B ships; direct browser writes are not trusted. Owner-side setup steps, rollback SQL, and verification checklist are in `repo-research/SECURITY_3_SUPABASE_RLS_NOTES.md`.
- Security 4 phase — run (2026-06-16). Cloudflare Worker proxy for the n8n webhook submission path. Worker source `workers/n8n-form-proxy.ts`. The four public forms (contact, quote, booking, newsletter) now POST to the forms Worker (`NEXT_PUBLIC_FORMS_WORKER_URL`) instead of directly to the n8n webhook URL. The Worker adds the per-form secret header server-side (`X-CodeOutfitters-Form-Secret`) and forwards to the correct n8n webhook URL. The n8n URLs and the per-form secrets are server-side only. `NEXT_PUBLIC_N8N_WEBHOOK_URL` is deprecated and forbidden. R-005 / R-017 addressed at the deployment level. F-006 implemented at the deployment level. Deferred at the runtime level until the owner deploys the forms Worker and configures the n8n workflow to verify the header. No `NEXT_PUBLIC_*_SECRET` env var was added. No n8n secret appears in browser code. The Worker source uses native `fetch` only; no npm dependencies. CSP `connect-src` updated: `https://*.n8n.io` removed; `https://*.workers.dev` already in the CSP from Security 1 covers the forms Worker. No Cloudflare Access app or Supabase migration applied; both are owner-side pre-launch actions. Setup steps are in `repo-research/SECURITY_4_N8N_SECRET_NOTES.md` §7.
- **Booking A phase — run (2026-06-16); SQL applied and verified at runtime by owner (2026-06-16); Booking A Repair 1 passed and applied to on-disk migration; Booking A live grant repair applied and verified.** The booking read path is now wired through a narrow RPC, `public.get_available_slots(p_month int, p_year int)`, that anon can invoke under Security 3 RLS. The SQL migration is in `supabase/migrations/20260616_booking_a_get_available_slots.sql`. **The migration was applied and verified at runtime by the owner** (confirmed 2026-06-16) after the on-disk migration was repaired to quote `"time"` (Booking A Repair 1, 2026-06-16). The migration required Repair 1 because the Supabase SQL Editor rejected the unquoted `time text` column with `ERROR: 42601: syntax error at or near "time"`. The repaired migration quotes `"time"` in `RETURNS TABLE`, in the inner `SELECT` (`s."time" AS "time"`), and in `ORDER BY` (`s."time" ASC`). The function is `SECURITY DEFINER`, filters `is_booked = false` server-side, returns only `id`, `date`, `"time"` ordered by date then time. Anon is granted `EXECUTE`; anon is **not** granted `SELECT` on the underlying `available_slots` table. Anon is **not** granted `EXECUTE` on `reserve_slot` (which does not exist yet; Booking B). **Booking A live grant repair was applied and verified at runtime by owner**: the owner revoked any broad direct table privileges from `anon` and `authenticated` on `public.available_slots` and `public.bookings`; revoked `authenticated` `EXECUTE` on `public.get_available_slots`; and restored the intended grants (`anon` `EXECUTE`, `service_role` `EXECUTE`). Final verification confirmed: `anon` has `EXECUTE` on `get_available_slots`; `service_role` has `EXECUTE` on `get_available_slots`; `authenticated` does **not** appear in the RPC grants; `anon` has no direct privileges on `available_slots`; `anon` has no direct privileges on `bookings`. The frontend `lib/booking-actions.ts` `getAvailableSlots(month, year)` calls the RPC. The frontend `components/booking-calendar-custom.tsx` calls `getAvailableSlots(month, year)` for the displayed month and disables dates and times that are not in the response. The submit handler is unchanged (still posts to the n8n forms Worker with `type: "booking"`). The UI design, step indicator, form fields, placeholder text (`+1 (555) 123-4567`), validation, honeypot, and type field are unchanged. R-005 is **partially closed at the read path level** and **verified at the runtime level**; R-005 is **fully closed** only when Booking B ships. F-004 is **implemented for the read path** and **verified at the runtime level**; F-004 is **fully closed** only when Booking B ships. The write path (`createBooking` in `lib/booking-actions.ts`) is intentionally unchanged in Booking A; the direct `INSERT` into `bookings` and the direct `UPDATE` on `available_slots.is_booked` now fail with 403 / RLS violations (Security 3 is in place). The transactional reservation path (`reserve_slot` RPC, called server-side from a Cloudflare Worker that holds the `service_role` key) is gated to Booking B. No new env var of any kind was added. No `service_role` key in any `NEXT_PUBLIC_*` env var, in the static bundle, in the repo, or in any client-reachable file. No `package.json` change. No lockfile change. No `next.config.*` change. No `public/_headers` change. No Worker change. No `app/**` change. **Status post-runtime:** R-005 is partially closed (read path); F-004 is implemented (read path); R-005 / F-004 are fully closed only when Booking B ships. Owner-side setup steps, RPC contract, verification queries, and a rollback plan are in `repo-research/BOOKING_A_AVAILABLE_SLOTS_RPC_NOTES.md`. **Known non-blocking issue:** the `"time"` column is `text` and the RPC returns it sorted lexicographically (so `1:00 PM` can appear before `10:00 AM`). Recorded as a future minor repair / Booking B-adjacent cleanup; do not start that repair unless explicitly approved.
- Coding — blocked on A0.
- All package installs — blocked.
- All tooling installs — not approved.
- All design/motion implementation — blocked. The owner's premium-motion direction (D-011) and design-taste skills (D-012) are captured for a future **UIX0 / MOTION0** phase. No code, no Impeccable install, no Emil Kowalski install, no MCP setup, no `npx skills add`, no `npx impeccable install` in this phase.
- **Ponytail** install / clone / config / evaluation — not approved; gated to TS0 / RDG0. Owner must provide the exact official GitHub repo URL, pinned version, and scope (default: reference-only) before any evaluation.
- **ECC / affaan-m/ecc** install / clone / copy / config / evaluation — not approved; gated to TS0 / RDG0. Owner must provide the exact official GitHub repo URL, pinned version, and scope (default: reference-only) before any evaluation.

## What must not happen next (without explicit approval)

- Editing any file under `app/`, `components/`, `lib/`, `hooks/`, `public/`, `*.config.*`, `package.json`, `tsconfig.json`, or either lockfile.
- Running `npm install`, `pnpm install`, or any package manager command.
- Installing Playwright, Playwright MCP, Chrome DevTools MCP, Graphify, Repomix, Context7 MCP, Tree-sitter, codebase-memory MCP, **Ponytail** (candidate only; not approved; gated to TS0 / RDG0), or **ECC / affaan-m/ecc** (candidate only; not approved; gated to TS0 / RDG0).
- Deleting either lockfile.
- Moving the Anthropic API behind a server function.
- Fixing the booking double-booking logic.
- Starting Security 5, Booking A, Booking B, Observability, QA Slice 0..3, UIX0 / MOTION0 Planning, UIX0 / MOTION0 first slice, UIX0 / MOTION0 later slices, Admin future, or Final QA / delivery.
- Editing `README.md` or deleting `DEPLOY.md`.
- Running `git add`, `git commit`, `git push`, or any other git-changing command (until the owner explicitly approves per occurrence; the first commit is now OPTIONAL, not a precondition for Cleanup A; default is no commit until final delivery).
- Re-initializing git. The repo is already initialized with `git init -b main`. Do not run `git init` again. Do not run `git init` in any sub-directory.
- Adding a remote. `git remote add` is NOT approved. No GitHub repo. No publishing.
- Pushing. `git push` is NOT approved. No push to any remote at any time, including final delivery, without explicit owner approval.
- Creating a GitHub repo. The agent does not create GitHub repos. The owner creates the GitHub repo at final delivery if and when the owner decides.

## Exact next gate after A0

**ChatGPT Control Room approval** of `docs/A0_ACTION_PLAN.md`, `repo-research/A0_PHASE_EXECUTION_QUEUE.md`, `repo-research/A0_AGENT_ASSIGNMENT_MATRIX.md`, and `repo-research/A0_CHANGE_ZONE_MAP.md` (or a repair / reject decision).

A0 review options:
- **Approve:** the recommended defaults and the PD1 LOCKED DEFAULTS stand. IMPL phases may proceed against the accepted A0 plan.
- **Repair:** A0 returns to refine the plan based on reviewer notes (e.g., add a new phase, change a default, narrow an agent responsibility, change a file-zone classification).
- **Reject:** halt the action / build plan path; revisit the brief.

A0 stops and waits for the review decision before any IMPL phase starts.

## Exact next gate after Setup

**ChatGPT Control Room approval of Setup and A0.** The first commit is now **OPTIONAL**, not a precondition for Cleanup A. The owner may commit locally at any time (per `repo-research/SETUP_FIRST_COMMIT_PLAN.md` §8) for rollback safety, or defer all commits to final delivery. The agent does not commit on the owner's behalf. The agent does not push, does not add a remote, and does not create a GitHub repo. After Setup + A0 approval, Cleanup A is the next phase (per A0 plan §5.2 and the A0 phase execution queue #2).

## Exact next gate after Cleanup A

**ChatGPT Control Room approval of Cleanup A.** Cleanup A ran (2026-06-16): README repaired; root `DEPLOY.md` deleted; portfolio copy truth fixed; contact form `source: "contact"` added; integration notes updated; `.gitignore` confirmed clean; ESLint config deferred. The next phase is Cleanup B (lockfile cleanup per D-015), gated to ChatGPT Control Room approval of Cleanup A and to D-015 answer. The first commit remains OPTIONAL.

## Exact next gate after Cleanup B

**ChatGPT Control Room approval of Cleanup B.** Cleanup B ran (2026-06-16): `pnpm-lock.yaml` deleted; `package-lock.json` remains as the canonical lockfile; `pnpm-lock.yaml` listed in `.gitignore`; `package.json` untouched; documentation updated; R-002 closed. The next phase is Security 1 (Worker proxy for Anthropic per D-019a), gated to ChatGPT Control Room approval of Cleanup B and to D-019a owner answer. The CI re-entry guard (asserting `pnpm-lock.yaml` is absent) is gated to a future TS0 / RDG0 phase. The first commit remains OPTIONAL.

## Exact next gate after Security 1

**ChatGPT Control Room approval of Security 1.** Security 1 ran (2026-06-16): Worker source `workers/anthropic-proposal-proxy.ts` shipped; frontend `lib/proposal-generator.ts` no longer references `NEXT_PUBLIC_ANTHROPIC_API_KEY`; `NEXT_PUBLIC_PROPOSAL_WORKER_URL` is the new public env var; `ANTHROPIC_API_KEY` is the new server-side secret bound to the Worker; CSP `connect-src` updated; R-002 closed at the runtime level; F-001 implemented. The Worker needs to be deployed to Cloudflare by the owner before the frontend can use it; deployment steps are in `repo-research/SECURITY_1_WORKER_PROXY_NOTES.md` §5. The next phase is Security 2 (admin auth per D-020a — Cloudflare Access for fast internal protection, or Supabase Auth / magic link if admin becomes productized), gated to ChatGPT Control Room approval of Security 1 and to D-020a owner answer. CORS is not authentication; the Worker is still gated to Security 2 before any non-internal launch. The first commit remains OPTIONAL.

## Exact next gate after Security 2

**ChatGPT Control Room approval of Security 2** and **the owner creating the Cloudflare Access app** in the Cloudflare Zero Trust dashboard. Security 2 ran (2026-06-16): `app/admin/layout.tsx` convenience gate explicitly labeled as such; `NEXT_PUBLIC_ADMIN_PASSWORD` deprecated as security; R-001 addressed at the deployment level; F-002 implemented for the deployed path; Worker doc comment updated to document the Security 2 boundary and the Worker-level JWT verification follow-up. The next phase is Security 3 (Supabase RLS per D-020 — `ENABLE ROW LEVEL SECURITY` on `bookings` and `available_slots`; anon `USING (false)`; explicit narrow grants for the `get_available_slots` and (post-Worker) `reserve_slot` RPCs; `service_role` retains full access). Security 3 is gated to ChatGPT Control Room approval of Security 2 and to D-020 owner answer. The first commit remains OPTIONAL.

## Exact next gate after Security 3

**ChatGPT Control Room approval of Security 3** and **the owner applying the SQL migration** at `supabase/migrations/20260616_security3_rls.sql` via the Supabase SQL editor (recommended) or the Supabase CLI (alternative). Security 3 ran (2026-06-16): SQL migration written; **NOT applied**; R-003 closed at the SQL level; F-003 implemented at the SQL level. The migration is idempotent and reversible; rollback SQL is in `repo-research/SECURITY_3_SUPABASE_RLS_NOTES.md` §6. After the owner applies the migration, the booking flow is non-functional by design (direct browser writes are denied); the fix is Booking A (RPC read path) and Booking B (RPC write path via the Worker). The next phase is Security 4 (n8n per-form secret per the bundled Q-19 / D-022 / R-005; each form POSTs with a header containing the per-form secret; n8n verifies). Security 4 is gated to ChatGPT Control Room approval of Security 3 and to the owner applying the Security 3 migration. The first commit remains OPTIONAL.

## Exact next gate after Security 4

**ChatGPT Control Room approval of Security 4** and **the owner deploying the forms Worker** to Cloudflare and **configuring the n8n workflow** to verify the `X-CodeOutfitters-Form-Secret` header. Security 4 ran (2026-06-16): forms Worker source `workers/n8n-form-proxy.ts` shipped; four forms updated to call the Worker; CSP `connect-src` updated; R-005 / R-017 addressed at the deployment level; F-006 implemented at the deployment level. Deferred at the runtime level until the owner deploys the forms Worker and configures the n8n workflow. The next phase is Booking A (read path through `get_available_slots` RPC, gated to the owner applying the Security 3 migration). Booking A is gated to ChatGPT Control Room approval of Security 4. The first commit remains OPTIONAL.

## Git push / commit policy (2026-06-16) — Control Room correction

- The owner wants the project pushed one time at the end. No chunked pushes. No remote until final delivery.
- `git init` already happened locally (`git init -b main` at `F:\CodeOutfitters`). That init is accepted. Do not re-init.
- `git push` is NOT approved. `git remote add` is NOT approved. No GitHub repo. No publishing.
- First baseline commit is OPTIONAL, not required before Cleanup A.
- Cleanup A may proceed after ChatGPT Control Room approval without a baseline commit, if the owner wants one final commit only.
- Local commits are owner-driven. The agent does not run `git add`, `git commit`, `git push`, `git remote add`, `git fetch`, or `git pull` from this batch onward without explicit owner approval per occurrence.
- The full policy is recorded in `PROJECT_CONTROL_LOG.md` under "Git push / commit policy update (2026-06-16) — Control Room correction."

## Exact next gate after Booking A

**Booking A is applied and verified at runtime (2026-06-16).** Security 3 is applied and verified at runtime (2026-06-16). Booking A Repair 1 (quoting `"time"`) is applied to the on-disk migration and was applied to the database. Booking A live grant repair is applied and verified at runtime. R-003 is closed end-to-end. R-005 is partially closed at the read path level and verified at the runtime level. F-004 is implemented for the read path and verified at the runtime level. The booking calendar on `/book` reads availability through the `get_available_slots` RPC. The booking form's n8n POST to the forms Worker (Security 4) is unchanged. The write path (`createBooking` in `lib/booking-actions.ts`) is intentionally unchanged and now fails with 403 / RLS violations because Security 3 RLS is in place. The next phase is **Booking B** (A0 future phase #9; the `reserve_slot` RPC + Worker-mediated transactional reservation). **Booking B is blocked until ChatGPT Control Room issues the exact Booking B prompt.** The first commit remains OPTIONAL.

## Exact next gate after Booking B

**Booking B is applied and verified at runtime (2026-06-16).** Security 3 is applied and verified at runtime (2026-06-16). Booking A is applied and verified at runtime (2026-06-16). The owner applied the Booking B SQL migration at `supabase/migrations/20260616_booking_b_reserve_slot.sql` via the Supabase SQL editor on 2026-06-16. The owner deployed the Booking Worker at `workers/booking-reservation-worker.ts` on 2026-06-16 via the Cloudflare dashboard paste of `workers/booking-reservation-worker.dashboard.js` (the `.ts` source was rejected by the dashboard parser; the JS dashboard copy is the dashboard-paste form — Booking B Repair 1, 2026-06-16). The required Worker env vars (`ALLOWED_ORIGIN`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`) were configured; n8n vars (`N8N_BOOKING_WEBHOOK_URL`, `N8N_BOOKING_SECRET`) were intentionally left for later. The Worker smoke test passed: `bookingId = 69e071f0-8954-4da1-bcb4-75f272bd2b87` with `notification = "skipped"`. Supabase verification confirmed the booking row was created in `public.bookings` for `preferred_date = 2026-06-17` / `preferred_time = '10:00 AM'`; `available_slots.is_booked = true` for the same `(date, time)`. A duplicate booking test returned `slot_already_booked` (`P0001`). **R-005 is fully closed at the runtime level**; **F-004 is fully closed at the runtime level**. The frontend `lib/booking-actions.ts:createBooking` posts to `${NEXT_PUBLIC_BOOKING_WORKER_URL}/` and preserves the `ActionResult<null>` contract. The frontend `components/booking-calendar-custom.tsx:handleSubmit` calls `createBooking(formData)`. The UI design is unchanged. No `NEXT_PUBLIC_*_SECRET` env var was added. No `service_role` key is in any `NEXT_PUBLIC_*` env var, in the static bundle, in the repo, or in any client-reachable file. The `service_role` key is server-side only and is bound only to the Worker. The reservation path is server-side only. The browser never calls `reserve_slot` directly. `reserve_slot` is `service_role` only. The function signature is `public.reserve_slot(p_date date, p_time text, p_booking jsonb) returns uuid`. The migration is idempotent and reversible; rollback is in `repo-research/BOOKING_B_RESERVATION_RPC_WORKER_NOTES.md` §11. The Worker is a thin proxy plus a strict CORS gate; CORS is not authentication. The Worker source uses native `fetch` only; no npm dependencies. The Worker is exposed to anyone who can match an `ALLOWED_ORIGIN` entry from a browser they control; the booking form's honeypot is the only bot protection. **Remaining deployment gap (informational, not a Booking B defect):** the Cloudflare Pages project is connected to GitHub; the current Pages deployment is still old GitHub code (pre-Booking-B frontend). The Worker is end-to-end functional at the Worker level (smoke test passed; Supabase verification passed); the browser-driven end-to-end flow cannot be live-tested through Pages until the updated local code is pushed/deployed. **Git push / remote remains blocked** until the owner explicitly approves final delivery deploy (per the 2026-06-16 Control Room correction on git push / commit policy). The next phase is **Observability** (A0 future phase #10; Sentry + UptimeRobot + email per D-026). Observability is **blocked** until ChatGPT Control Room issues the exact Observability prompt. The first commit remains OPTIONAL.

## Live browser verification (2026-06-18)

**Booking A live availability: passed.** The live site at `https://codeoutfitters.pages.dev/book` loads available slots via the `get_available_slots` RPC. July 2026 slots render in the date picker.

**Worker 204 OPTIONS repair: passed.** The Booking Worker's OPTIONS/preflight handler was fixed to return `new Response(null, { status: 204, headers })` instead of `jsonResponse({}, 204, ...)`, which caused Worker error 1101. The browser preflight now completes successfully.

**Booking B live browser booking: passed.** A live booking was submitted through the browser and confirmed:
- Supabase `public.bookings`: name `Tayyab Sana Ullah`, email `aboolography@gmail.com`, preferred_date `2026-06-18`, preferred_time `3:00 PM`
- Supabase `public.available_slots`: date `2026-06-18`, time `3:00 PM`, is_booked `true`
- UI displayed "Booking Confirmed!"

**n8n/email notification: not configured.** The Worker's n8n forward is intentionally left for later. The booking RPC is the source of truth; the operator notification is a nice-to-have. The booking success copy was updated to reflect this: "Your booking request has been received. We'll follow up with the details." (removed the promise of email delivery.)

**Next recommended phase:** Observability, then UIX0/MOTION0.

## Booking B Repair 1 (2026-06-16) — dashboard JS copy

**Issue:** the owner pasted `workers/booking-reservation-worker.ts` into the Cloudflare dashboard Worker editor and received `Uncaught SyntaxError: Unexpected strict mode reserved word at worker.js:178`. Root cause: the dashboard editor parses pasted code as JavaScript, not TypeScript; the `.ts` source contains `interface Env` (line 178) and other TypeScript-only syntax.

**Repair shipped (2026-06-16):** `workers/booking-reservation-worker.dashboard.js`. 1:1 runtime port of the TypeScript Worker. All TypeScript-only syntax removed (`interface`, type annotations, type unions, type intersections, type predicates, `as` casts). All runtime logic preserved byte-for-byte (CORS gate, payload validation, Supabase REST call, response shape, n8n forward, error mapping). All env bindings preserved exactly. Service-role key handling preserved (only in `apikey` and `Authorization: Bearer` request headers). Supabase RPC path preserved (`${SUPABASE_URL}/rest/v1/rpc/reserve_slot`). Worker module format preserved (`export default { async fetch(request, env) { ... } }`).

**Two deployment paths:**

- **`wrangler` CLI** (recommended for TypeScript-aware deployment): use `workers/booking-reservation-worker.ts`. `wrangler` accepts `.ts` directly via esbuild. The TypeScript Worker is the source of truth.
- **Cloudflare dashboard paste** (when the owner prefers GUI or when `wrangler` is unavailable): use `workers/booking-reservation-worker.dashboard.js`. Do **not** paste the `.ts` source into the dashboard; it will fail with the same syntax error.

**State files updated:** `repo-research/BOOKING_B_RESERVATION_RPC_WORKER_NOTES.md` (§14 appended), `PROJECT_CONTROL_LOG.md` (Booking B Repair 1 overlay appended), `memory/CURRENT_STATE.md` (this note), `docs/51_AGENT_HANDOFF_LOG.md` (Booking B Repair 1 handoff entry appended).

**Files NOT modified:** `workers/booking-reservation-worker.ts` (unchanged; the source of truth). `supabase/migrations/20260616_booking_b_reserve_slot.sql`, `lib/booking-actions.ts`, `components/booking-calendar-custom.tsx`, `.env.local.example` (unchanged). `package.json`, lockfiles, `tsconfig.json`, `next.config.*`, `postcss.config.*`, eslint / tailwind configs (unchanged). The other two Workers (unchanged). All other files (unchanged).

**Hard rules reaffirmed:** no SQL applied; no Worker deployed by OpenCode; no package commands; no `git add` / `commit` / `push`; no service-role key exposed; no Booking B SQL changed; no new phase started. The repair is one new file + state updates only. R-005 and F-004 status is unchanged (still code-shipped at the write path level; still deferred at the runtime level until the owner completes the Booking B owner-action steps). Observability is still the next eligible phase; still blocked.
